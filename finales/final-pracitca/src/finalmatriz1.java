/*Se tiene dos matrices MatA y MatB de NxM de tipo caracter. En cada fila de MatA y MatB hay una o más secuencias
de letras distintas del caracter espacio, separadas por uno o más caracteres espacios.
Se pide agregar la mayor cantidad de secuencias no repetidas de MatB en MatA, considerando que dichas
secuencias no existan en MatA, que al agregar una secuencia en una fila de MatA se está reemplazando caracteres
espacios consecutivos por una secuencia de letras distintas del carácter espacio, donde además la nueva secuencia
debe quedar separada de las ya existentes.
Observaciones generales:
_Suponer que MatA y MatB están cargados.
_En ambas matrices puede haber uno o más caracteres espacio al principio y al final de la fila.
_Realizar el programa completo bien modularizado.*/


/*char secuencia [];
*maxcol=n
*maxfil=n
 * char matA [maxcol][maxfil]
 * char matB [maxcol][maxfil]
 * contar_lugares(){
	int primer_posicion;
 * int tamaño_secuencia
 * int posicion_fila;
 * 
 * for(fila, fila, fila++){
 * 		for (columna, columna, columna++){
		if(matB[columna][fila]!=" "){
			secuencia[tamaño_secuencia]=matB[columna][fila];
				
				if(tamaño==0) {
					primer_posicion=j;
					posicion_fila=i;
				}				
			tamaño_secuencia++;
 * 			comparar(primer_posicion,posicion_fila,tamaño_secuencia);
			}
		else{
			primer_posicion=0;
 * 			tamaño_secuencia=0;
 * 			posicion_fila=0;
			}
		}
		
 * 	}
 * }
 * comparar(primer_posicion,posicion_fila,tamaño_secuencia){
 * 	int contador;
 * char auxiliar[];
 * 		for(fila, fila, fila++){
 * 			for (columna, columna, columna++){
				if(matA[columna][fila]!=" "){
				contador++
					if((columna==maxcol-1||matA[columna+1][fila]==" ")&&contador==tamaño_secuencia){
						int contador
						for(i,tamaño_secuencia,i++){
							
								if(secuencia[i]!=maxA[(columna-(contador-1))+i][fila]){
									insertar();
									break;
									}
												
							}
						}
				
				}
				
				
		}
		
 * 
 *  }
 * */
public class finalmatriz1 {

	public static void main(String[] args) {
		
	}

}
